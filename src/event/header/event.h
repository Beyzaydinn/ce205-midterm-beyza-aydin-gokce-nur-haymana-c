/**
 * @file event.h
 * 
 * @brief Provides functions for math. utilities
 */

#ifndef EVENT_H
#define EVENT_H

#include "../../utility/header/commonTypes.h"

bool mainMenu();
bool authentication();
bool mainMenu();
bool eventDetails();
bool attendee();
bool schedule();
bool feedback();
bool Register();
bool logIn();
bool guest();
bool displayUsers();


#endif // EVENT_H